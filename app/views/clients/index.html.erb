<h1>Clients</h1>

<% if @clients.any? %>

<table>
  <tr>
    <th>Full name</th>
  </tr>

  <% @clients.each do |client| %>
    <tr>
      <td><%= link_to client[:fullname] , action: :show, id: client[:id] %></td>
    </tr>
  <% end %>
</table>

<% else %>
  <p>No clients</p>
<% end %>
